'use client';

/**
 * Registry Store Proxy
 * Centralizes state management for cart, region, and favorites.
 * Redirects to the TSX context provider to ensure unified state across the registry.
 */

export { useStore } from './store.tsx';
